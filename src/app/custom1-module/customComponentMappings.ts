import { TestAlexComponent } from '../test-alex/test-alex.component';

// Define the map
export const selectorComponentMap = new Map<string, any>([
    ['nde-record-availability-bottom', TestAlexComponent]
]);
